version: "1.0.0"
name: parquet_test
owner: test-team
description: Parquet format contract for testing

schema:
  format: parquet
  location: s3://test/parquet
  fields:
    - name: id
      type: string
      nullable: false
    - name: value
      type: int
      nullable: true
